# レビューゲート記録 (docs/reviews/)

line-hf-bot の**4段階レビューゲート**の判定記録を残す場所。各ゲートは薄いラッパのサブエージェント
（`.claude/agents/`）が担い、実分析は既存プラグインに委譲する。

## ゲートと担当エージェント
| 順 | ゲート | エージェント | 委譲先（分析エンジン） |
|---|---|---|---|
| 1 | 仕様レビュー | `spec-review-gate` | なし（自前） |
| 2 | 実装レビュー | `impl-review-gate` | `dotnet-claude-kit:code-review`（Roslyn MCP） |
| 3 | セキュリティレビュー | `security-review-gate` | `dotnet-claude-kit:security-scan`（＋`claude-security`/`42crunch`） |
| 4 | ドキュメントレビュー | `doc-review-gate` | なし（自前） |

## 運用ルール
- **起動**: オンデマンド（「仕様ゲート回して」等の指示、または各フェーズ完了時）。将来は機能/PR 単位でも同じエージェントを流用。
- **強制度**: 記録付きソフトゲート。**FAIL は既定でブロック**（次ゲートに進まず差し戻し）。ユーザーが明示すれば上書き可。
- **順序**: 仕様 → 実装 → セキュリティ → ドキュメント。各ゲート PASS で次へ。

## ファイル命名
`<連番2桁>-<gate>-<対象スラッグ>.md`
例: `01-spec-initial-scaffold.md` / `02-impl-image-generation.md` / `03-security-webhook.md`

## 記録テンプレート
```
# <ゲート名> — <対象> (<YYYY-MM-DD>)
Verdict: PASS | FAIL
委譲分析: <実行したプラグイン/スキル、無ければ「なし（自前）」>

## チェックリスト / 指摘
| # | 重大度 | 箇所 | 問題 | 必要な対応 |

## 判定理由
<根拠。FAIL なら差し戻すべき項目を明示>
```

## 記録インデックス
<!-- 新しい判定を上に追記する -->
- 2026-08-18 **PASS** ドキュメントレビュー（vision フォローアップ / 会話型 vision / spec09）— [`48-doc-vision-followup-multiturn.md`](48-doc-vision-followup-multiturn.md)（Blocker 0、Major 2＝本レビュー後に修正済み、Minor 1。`App__VisionMaxTurns=8` の六者一致(BotOptions/appsettings.json/.env.example/README×2/CLAUDE.md)を確認。会話型 vision のクレジット消費・離脱導線・Part1 の Ask ボタン gate・`VisionAnswer` QR が実装と一致、en/ja 文言・英語コメント規約順守、秘密情報なし。Major#1=README 要約文の画像結果ボタン列挙に 💬Ask 欠落(自己矛盾)→EN/JA 修正、Major#2=docs/deploy/docker-hub{,.ja}.md の App パラメータ表に `App__VisionMaxTurns` 欠落→追記。spec09 は4ゲート全PASS=45/46/47/48）
- 2026-08-18 **PASS** セキュリティレビュー（vision フォローアップ / 会話型 vision / spec09）— [`47-security-vision-followup-multiturn.md`](47-security-vision-followup-multiturn.md)（Critical/High/Medium 0、Info 3、脆弱0。委譲=dotnet-claude-kit:security-scan 6層／42crunch・claude-security 不適用(新規 API/OpenAPI/エンドポイントなし)。署名検証 不変(差分は検証後の Dispatcher ルーティング＋状態のみ)。トークン漏洩なし＝Bearer は VisionEndpoint(router 固定)のみ・結果 URL 再取得なし・ログは kind/user/eventId のみ。新規 SSRF 面なし＝履歴＋画像 data URI を router へ送るだけ。IDOR なし＝follow-up の RefImageId はサーバ側 VisionImageId/LastImageId 由来でユーザー入力が id にならない。DoS＝VisionMaxTurns(8)で per-user ターン上限・Math.Max(1,…)丸め・超過先頭破棄・画像はターン非保存で毎回 MediaStore 取得＝有界・期限切れ Clear。Info=毎ターン画像+履歴再送でクレジット増(上限で受容)/worker read-append 非原子(セキュリティ影響なし)/状態退避なし(既存性質)）
- 2026-08-18 **PASS** 実装レビュー（vision フォローアップ / 会話型 vision / spec09）— [`46-impl-vision-followup-multiturn.md`](46-impl-vision-followup-multiturn.md)（Blocker/Major 0、Minor 3。委譲=dotnet-claude-kit:code-review/Roslyn。変更ファイル diagnostics 0/0/0・spec09 由来の新規 antipattern 0（検出分は既存 catch とテスト慣行のみ）。受入基準13項目すべて充足＝AC3 マルチターン組み立て(画像は先頭 user ターンのみ・以降 text のみ、`VisionService.BuildMessages`)、AC5 履歴 cap(`AppendVisionTurn` Math.Max(1,maxTurns)＋先頭破棄)、AC6 画像切替リセット(`GetVisionHistory` id 不一致→空)、AC7 失敗ターン非蓄積 en/ja(Theory 固定)、AC8 初回ヒント gate(`succeeded && firstTurn`)、AC10 セッション終了点(store: SetMode/SetLastImage/SetReceivedImage/Reset＋dispatcher: regen/edit/animate/slash の ClearVisionSession)、AC11 期限切れ Clear、AC12 既定8の三者一致(BotOptions/appsettings/.env.example/README×2/CLAUDE.md)を実装＋テストで確認。スレッド安全＝新規セッション操作は UserState ロック下・GetVisionHistory は ToArray コピー返却。ルーティング優先＝slash 先取り→Pending→セッション→モード が spec §4.3 と一致、pending+slash も挙動等価。SetLastImageId/SetPending 非 Clear は decision⑤/ask 再arm で正当・リークなし。エラー契約は spec07(ChatService 準拠)維持。build 0/0・test 131 緑。残 Minor=worker の read-append TOCTOU(設計内)/空白follow-up の空 Vision enqueue(既存モードと一貫)/失敗判定の文字列一致(spec 合意・将来構造化余地)）
- 2026-08-18 **PASS 相当** 仕様レビュー（vision フォローアップ / 会話型 vision / spec09）— [`45-spec-vision-followup-multiturn.md`](45-spec-vision-followup-multiturn.md)（初回 FAIL: Blocker 1（失敗初回ターンのヒント/継続導線の矛盾）＋Minor 2 → 指摘反映後 PASS 相当。決定④=初回ヒントは実際に AppendVisionTurn された初回成功ターンに gate・初回失敗はセッション未開始でワンショットへフォールバック、決定⑤=SetLastImageId は edit arm で解除済みのため Clear 対象外・失敗判定の文字列一致は現契約踏襲で en/ja テスト固定）
- 2026-08-17 **PASS** ドキュメントレビュー（画像→チャット vision/VQA / spec07）— [`40-doc-image-vision-vqa.md`](40-doc-image-vision-vqa.md)（Blocker/Major 0、Minor 1＝反映済み。Vision 4キー(VisionModel=Qwen/Qwen2.5-VL-7B-Instruct / VisionEndpoint=router /v1/chat/completions / VisionTimeoutSeconds=60 / App__VisionEnabled=true)が BotOptions.cs・appsettings.json・.env.example・README(EN/JA)・CLAUDE.md で完全一致。既定 ON で受信写真 UX が spec04 から変わる点・provider 依存で「質問」→汎用 Error・fal 非依存=チャット同等クレジット を全ドキュメントで明記。VisionService の /v1/chat/completions 直叩き/base64 data URI/ChatService 準拠エラー契約・PendingAction enum・DI 登録・エンドポイント(新規なし)が実装と一致。README 二言語対応・specs 一覧 07 追加・tech stack に vision・言語ルール順守・秘密情報なし。Minor=Commands 表「a photo」行が spec04 のまま(Features/Config 表と内部矛盾)→本レビューで EN/JA とも既定 vision オン挙動に修正。spec07 は4ゲート全PASS=37/38/39/40）
- 2026-08-17 **PASS** セキュリティレビュー（画像→チャット vision/VQA / spec07）— [`39-security-image-vision-vqa.md`](39-security-image-vision-vqa.md)（Critical/High/Medium 0、Informational 2、脆弱0。委譲=dotnet-claude-kit:security-scan 6層／42crunch・claude-security 不適用(新規 HTTP API 面・OpenAPI なし)。署名検証は不変(vision は検証後 DispatchAsync 分岐のみ=action=ask postback／Pending==VisionQuestion)。トークン漏洩なし＝Bearer は VisionEndpoint(router)のみ・AllowAutoRedirect=false・ログは kind/user/eventId のみで token/画像本文/data URI/質問文 非露出。新規 SSRF 面なし＝結果 URL 再取得せず送信先 router 固定・data URI はユーザー自身の写真(spec04 の MaxIncomingImageBytes/timeout/external 拒否で取得済み)。DoS 制御＝VisionTimeoutSeconds(60) linked CTS(下限5s)＋Queue 有界(100)＋TryMarkNew 冪等。脆弱パッケージ0・新規依存なし。Info=base64 化で送信ボディ+33%(上限あり・受容)/最上位 catch の例外ログは HF 応答本文(500字トランケート・HF 由来・token 非含)。プロンプトインジェクションは vision 回答に閉じ外部 I/O 誘発なし）
- 2026-08-17 **PASS** 実装レビュー（画像→チャット vision/VQA / spec07）— [`38-impl-image-vision-vqa.md`](38-impl-image-vision-vqa.md)（Blocker/Major 0、Minor 4→2 解消。委譲=dotnet-claude-kit:code-review/Roslyn。detect_antipatterns は spec07 変更ファイル由来 0（VisionService.cs は OCE catch フィルタ付きで検出ゼロ）、diagnostics 0/0/0、DI captive 0。エラー契約＝ChatService 準拠を実装で確認（OCE→Timeout ガード付き・内部のみ／空→EmptyAnswer／非2xx のみ送出→最上位 catch(OCE除外)→Error、record 26 の無応答罠を回避）。AwaitingEdit→PendingAction enum 置換は reset 前 snapshot で分岐先決定＋先頭 SetPending(None) で二重解決なし、mode/regen/slash キャンセルを回帰テスト実証。受信写真は VisionEnabled で Pending=None+choices / Pending=Edit+ImageReceived を原子的分岐（AC6/7/13）。SSRF 面増なし・送信先 router のみ Bearer・AllowAutoRedirect=false。build 0/0・test 79 緑。レビュー後 AC10/11 の WorkProcessorVisionTests を追加し Minor#2/#3 解消。残 Minor=AC8 postback 直接テスト無し(action=edit 対称・PostbackEvent は外部モデルで推測構築回避)/LastImageId 空時 EditNoImage 文言）
- 2026-08-17 **PASS** 仕様レビュー（画像→チャット vision/VQA / spec07）— [`37-spec-image-vision-vqa.md`](37-spec-image-vision-vqa.md)（初回 FAIL=Blocker1/Major1/Minor3 → 反映 → 再判定 PASS。エラー契約を ChatService 準拠に一本化＝`AnswerAsync` は表示可能文字列を返し OCE→Timeout・空→EmptyAnswer をサービス側変換・非2xx のみ送出→最上位 catch(OCE除外)→Error（record 26 の罠回避）。`AwaitingEdit`→`PendingAction` enum 化。既定 `VisionEnabled=true` で受信 UX が spec04 から変わるため AC13 を「VisionEnabled=false 固定の現行検証＋新 UX 新テスト追加」へ訂正。SSRF 面なし・router のみ・PublicBaseUrl 不要。残 Minor 2＝§1 署名表記/§10 雛形役割併記＝反映済み）
- 2026-08-16 **PASS** ドキュメントレビュー（動画 text-to-video を fal-ai プロバイダ経由 / spec06）— [`36-doc-video-fal.md`](36-doc-video-fal.md)（Blocker/Major 0、Minor 1＝反映済み。VideoModel(fal-ai/wan/v2.2-5b/text-to-video)/VideoEndpoint(fal router?_subdomain=queue)/VideoTimeoutSeconds(300)/VideoEnabled(既定false) が BotOptions.cs と appsettings.json/.env.example/README(EN/JA)/CLAUDE.md/spec06§4 で過不足なく一致。fal 非同期キュー・共通 FalQueue・video.url 抽出・SSRF(MediaRefetch)・トークン router のみ・hf-inference 非対応 が VideoService.cs/FalQueue.cs と一致。VideoEnabled opt-in(fal 有料/遅い)が全ドキュメントで一貫。エンドポイント /webhook /media/{id} /health は Program.cs と一致、/dev/video は Development 限定。README 二言語対応・specs 一覧 01-06 追加・言語ルール順守・秘密情報なし。Minor=AppOptions.VideoEnabled の XMLドキュメントが「until a provider is wired」の旧 scaffold 表現のまま実態(fal 接続済み・opt-in理由=有料/遅い)と齟齬→本レビューで更新済み。spec06 は4ゲート全PASS=33/34/35/36）
- 2026-08-16 **PASS** セキュリティレビュー（動画 fal-ai プロバイダ経由 / spec06）— [`35-security-video-fal.md`](35-security-video-fal.md)（Critical/High/Medium 0、Low 1・Info 2、脆弱0。トークン漏洩ガード＝`FalQueue.ToRouterUrl` が `https://queue.fal.run/`(末尾スラッシュ込) 始まりのみ受理し router へ書換＝HFトークンは router.huggingface.co にのみ送出、他ホスト/http/サブドメイン偽装を Ordinal 前方一致で fail-closed 拒否、最終メディア取得は no-auth。SSRF＝`video.url` は Object/非空 fail-closed 抽出→`MediaRefetch`(https/allowlist ラベル境界/空=全拒否/no-auth)＋typed client `AllowAutoRedirect=false` で 3xx 迂回封鎖。共通コード `FalQueue`/`MediaRefetch` を image-edit と共有し記録31/34 の防御を video 経路へ引継ぎ(回帰なし)。webhook 署名 非接触(検証後 DispatchAsync→queue→HandleVideoAsync 不変)。DoS＝poll ~1s＋VideoTimeoutSeconds(300) linked CTS＋未知status早期例外、Queue 100/Workers 2 で有界、VideoEnabled 既定OFF。`/dev/video` は IsDevelopment ガード内。ログ/例外にトークン非混入(HfHttp 500字トランケートは HF応答由来)。残 Low＝refetch `ReadAsByteArrayAsync` 無上限(信頼 allowlist＋timeout＋既定OFF で抑制・将来 MaxResponseContentBufferSize 推奨、実装ゲート Minor#3 の再評価)。委譲: dotnet-claude-kit:security-scan / 脆弱0=`dotnet list package --vulnerable`）
- 2026-08-16 **PASS** 実装レビュー（動画 fal-ai プロバイダ経由 / spec06）— [`34-impl-video-fal.md`](34-impl-video-fal.md)（Blocker/Major 0、Minor 3。VideoService を hf-inference 同期→fal-ai 非同期キュー(submit `{prompt}`→router書換poll→`video.url`→`MediaRefetch(fal.media)`)へ全面書換、共通処理を `Ai/FalQueue.cs`(internal static)に抽出し image-edit と共有。`IVideoService` 契約不変。受入10項目充足＝トークン漏洩ガード(`ToRouterUrl` は `https://queue.fal.run/` 始まりのみ受理→router書換、HFトークンは router のみ・fail-closed)、SSRF(`MediaRefetch` https/allowlist/no-auth/timeout＋typed client `AllowAutoRedirect=false`、allowlist外拒否をテスト実証)、poll終了条件＋VideoTimeoutSeconds(300) linked CTS、`video.url` の Object/非空 fail-closed パース、dispose を確認。共通化で image-edit 回帰なし(`FalQueue.ToRouterUrl` 共有テスト緑)。裏取り＝diagnostics 0/0/0・変更ファイルの antipattern 0・`dotnet test` 64件緑(60+5video-1旧同期)。残 Minor=初回poll 1s遅延/`ToRouterUrl` Ordinal 大小区別(fail-close)/refetch サイズ上限なし(信頼ホスト・timeout抑制、securityゲートへ)。委譲: dotnet-claude-kit:code-review / Roslyn MCP）
- 2026-08-16 **PASS** 仕様レビュー（動画 fal-ai プロバイダ経由 / spec06）— [`33-spec-video-fal.md`](33-spec-video-fal.md)（Blocker/Major 0、Minor 2。VideoService を hf-inference 同期 → fal-ai 非同期キュー(submit `{prompt}`→router書換poll→`response_url` の `video.url`→`MediaRefetch(fal.media)`)へ書換、共通処理を `Ai/FalQueue.cs` に抽出し image-edit と共有。ワイヤ形式は huggingface_hub `FalAITextToVideoTask` で確定。`IVideoService` 契約不変＝`WorkProcessor`/`AllowAutoRedirect=false`/`/dev/video` 無改修をコードで裏取り。`MediaResponse` は ImageService で継続利用＝非orphan。トークンは router のみ・fal.media は no-auth+allowlist。`VideoEnabled` 既定OFF維持(fal有料)。残Minor=§4反映先に `BotOptions.cs` 明記/happy pathユニット検証の引き継ぎ）
- 2026-08-16 **PASS** ドキュメントレビュー（画像編集 fal-ai プロバイダ経由 / spec05）— [`32-doc-image-edit-fal.md`](32-doc-image-edit-fal.md)（Blocker/Major 0、Minor 1＝反映済み。ImageEditModel(fal-ai/qwen-image-edit)/ImageEditEndpoint(fal router ?_subdomain=queue)/ImageEditTimeoutSeconds(120) が BotOptions.cs と .env.example/README(EN/JA)/CLAUDE.md/spec05§4 で一致。fal 非同期キュー・hf-inference 非対応・トークンは router のみ・fal 有料 の記述が実装/テストと一致。README 二言語対応・言語ルール順守・秘密情報なし。Minor＝spec03(3b) の旧既定(Qwen/Qwen-Image-Edit+hf-inference)に spec05 への追従ポインタを追記して解消。spec05 は3ゲート全PASS=30/31/32）
- 2026-08-16 **PASS** セキュリティレビュー（画像編集 fal-ai プロバイダ経由 / spec05）— [`31-security-image-edit-fal.md`](31-security-image-edit-fal.md)（Critical/High/Medium 0、Low/Info 2、脆弱0。トークン漏洩ガード＝`ToRouterUrl` が `https://queue.fal.run/`(末尾スラッシュ込) 始まりのみ受理し router へ書換＝HFトークンは router.huggingface.co にのみ送出、evil ホスト/http/サブドメイン偽装を fail-closed で拒否(テスト実証)。SSRF＝最終 images[0].url は MediaRefetch(https/allowlist/no-auth)＋typed client AllowAutoRedirect=false、非allowlist拒否をテスト実証、入力は自前 dataURI 化で外部fetchなし。webhook署名 非接触。poll は linked-CTS(既定120s)＋1s間隔＋未知status早期例外。dev `/dev/imageedit` は IsDevelopment ガード内。残 Low＝応答 ReadAsByteArrayAsync 無上限(信頼ホスト・timeout緩和・既存共通)。委譲: dotnet-claude-kit:security-scan）
- 2026-08-16 **PASS** 実装レビュー（画像編集 fal-ai プロバイダ経由 / spec05）— [`30-impl-image-edit-fal.md`](30-impl-image-edit-fal.md)（Blocker/Major 0、Minor 3。原因=hf-inference が image-to-image 非対応→fal-ai 非同期キュー(submit→router書換poll→result→fal.media 取得)へ全面書換、IImageEditService 契約不変で上位無改修。トークン漏洩ガード・poll終了条件・linked-CTS タイムアウト・fail-closed JSON パース・dispose・AllowAutoRedirect=false を確認。受入9項目充足・diagnostics 0/0/0・テスト60件緑・実機E2E(1.45MB PNG)成功。残 Minor=初回poll 1s遅延/dev prompt nullable(修正済)/ToRouterUrl Ordinal 大小区別(fail-close)。委譲: dotnet-claude-kit:code-review / Roslyn MCP）
- 2026-08-15 **PASS** ドキュメントレビュー（README 整備・リポジトリ現状整合）— [`29-doc-readme-refresh.md`](29-doc-readme-refresh.md)（Blocker/Major 0、Minor 1＝修正済み。README(EN/JA) の Features/Commands/Configuration/Documentation/手順/エンドポイントが BotOptions.cs・.env.example・appsettings.json・docs/specs 01-04・docs/reviews と過不足なく一致。App__Locale/RichMenuEnabled/VideoEnabled・Line 受信画像2キー・ImageEditModel 等の既定値も一致。Minor=EN Documentation 節の CLAUDE.md リンク重複を1行へ統合し EN/JA 対応化。ゲートの CLAUDE.md 未反映指摘は誤検出＝spec04 実装時(4a10d5a)に反映済み）
- 2026-08-15 **PASS** ドキュメントレビュー（ユーザー画像の受信→image-to-image 編集 / spec04）— [`28-doc-user-image-edit.md`](28-doc-user-image-edit.md)（Blocker/Major 0、Minor 2＝反映済み。新2キー Line__MaxIncomingImageBytes(10485760=10MB)/Line__ContentFetchTimeoutSeconds(30) が BotOptions.cs と .env.example/README(EN/JA)/CLAUDE.md/spec§3 で一致。受信→Blob取得→保存→AwaitingEdit→img2img・external非対応(SSRF回避)・上限/タイムアウト・原子的 SetReceivedImage の記述が実装一致。en/ja 4文言＋help 揃い、README二言語対応、言語ルール順守、秘密情報なし。Minor＝spec のメソッド名 FetchAsync→FetchImageAsync／テスト件数 57件へ更新、いずれも本レビューで反映済み。spec04 は4ゲート全PASS=25/26/27/28）
- 2026-08-15 **PASS** セキュリティレビュー（ユーザー画像の受信→image-to-image 編集 / spec04）— [`27-security-user-image-edit.md`](27-security-user-image-edit.md)（Critical/High/Medium 0、Low 1・Info 2、脆弱パッケージ0。SSRF＝ユーザー由来 messageId は Kiota が固定ホスト api-data.line.me＋path エンコードで任意URL呼び出し不成立、`ContentProvider_type.External` は enqueue 前に拒否＝外部URL自前fetchなし。トークン非露出（新規ログは userId/messageId のみ、DescribeLineError は API バリデーション詳細のみ）。webhook 署名検証 不変・画像経路は検証後 DispatchAsync 追加で迂回なし・新規エンドポイントなし。DoS＝ReadCapped 書込前判定でメモリ有界＋linked-CTS タイムアウト＋キュー満杯拒否。IDOR＝GUID media id・per-user LastImageId・/media 既存GUID保護。残 Low＝取得タイムアウト上限キャップなし(設定由来・実効抑制あり)、Info＝blob client の redirect 無効化未明示(固定ホストで実害なし)/idempotency mark-before-fetch。委譲: dotnet-claude-kit:security-scan / 脆弱0=dotnet list package --vulnerable）
- 2026-08-15 **PASS** 実装レビュー（ユーザー画像の受信→image-to-image 編集 / spec04）— [`26-impl-user-image-edit.md`](26-impl-user-image-edit.md)（初回 FAIL→修正→再判定 PASS。Blocker/Major 0。初回 Major=AC#7 タイムアウト時 `ImageReceiveFailed` 未達＝取得タイムアウトの OCE が WorkProcessor の両 catch(`when not OCE`)をすり抜けユーザー無応答 → `FetchImageAsync` で自前タイムアウトを `TimeoutException`(非OCE)へ変換して解消(`ChatService` 同手法)。`SetReceivedImage` は3項目単一ロック原子更新、`ReadCappedAsync` はメモリ有界、DI は `ILineMessenger` と同型で captive なし、external 拒否で SSRF 面なし。受入12項目充足・diagnostics 0/0/0・テスト57件緑(既存53＋新規4=成功/上限/タイムアウト/一般失敗)。残 Minor=idempotency mark-before-fetch/意図的 empty-catch は受容可。委譲: dotnet-claude-kit:code-review / Roslyn MCP接続あり）
- 2026-08-15 **PASS** 仕様レビュー（ユーザー画像の受信→image-to-image 編集 / spec04）— [`25-spec-user-image-edit.md`](25-spec-user-image-edit.md)（初回 FAIL=Blocker1/Minor4 → 反映 → 再判定 PASS。Blocker は受信画像の本体取得の SDK 前提誤り＝`MessagingBlobApiClient` 直接注入不可・`.Api.V2` 誤り → `MessagingClient` 注入＋`client.Blob.V2.Bot.Message[id].Content.GetAsync(ct)` に是正。Minor=enum `ContentProvider_type` 判定/受信前テキストのレース文書化/`LineOptions` doc 更新/`WorkItem.Text`=messageId 追記。受入基準12項目テスト可能。スコープ=編集のみ、VQA・image→video は対象外）
- 2026-08-15 **PASS** セキュリティレビュー（画像編集 image-to-image・Qwen-Image-Edit / spec03 3b）— [`23-security-image-edit.md`](23-security-image-edit.md)（Critical/High/Medium 0、Low 2・Info 2、脆弱パッケージ0。img2img ペイロードはシリアライザ生成でインジェクション面なし。JSON-URL 再取得は共通 `MediaResponse`→`MediaRefetch` 経由で spec02 の SSRF 必須統制(https/allowlistラベル境界/フェイルクローズ/no-auth/timeout)＋`AllowAutoRedirect=false` を確実通過し記録20の統制が3b経路でも有効。`RefImageId` は完全にサーバ側 per-user `LastImageId` 由来でユーザテキスト/postback から任意 id 注入不可・他ユーザ横断不可(MediaStore id=GUID)。指示/プロンプト/ApiKey はログ非出力、`.env` gitignore。残 Low＝指示長上限なし(キュー100/timeout/直列配信で実効抑制)・`/media` は GUID 保護済み IDOR。残 Info＝AwaitingEdit 非アトミック消費は eventId 冪等排除で増幅経路にならず無視可。委譲: dotnet-claude-kit:security-scan / 脆弱0=`dotnet list package --vulnerable`）
- 2026-08-15 **PASS** 実装レビュー（画像編集 image-to-image・Qwen-Image-Edit / spec03 3b）— [`22-impl-image-edit.md`](22-impl-image-edit.md)（Blocker/Major 0、Minor 3。ビルド0警告/0エラー・Roslyn診断0/0/0・テスト緑(既存36＋新規9)。img2img payload=base64(inputs)+parameters.prompt をテスト実証、共有 `MediaResponse.ReadAsync` リファクタは image/video の payload 不変で回帰なし・全 typed client に `AllowAutoRedirect=false`。AwaitingEdit の消費/キャンセル/`/reset`クリアは AC#5/6/7 と整合、参照画像 TTL 失効は `EditImageExpired` でユーザ通知、UserStateStore 新メソッドは per-user lock で並行安全、秘密情報非露出。残 Minor＝AwaitingEdit の非アトミック test-and-clear の理論的競合／既存の意図的 empty-catch(WorkProcessor:72)／Dispatcher 直接テスト欠如。委譲: dotnet-claude-kit:code-review / Roslyn MCP接続あり）
- 2026-08-15 **PASS** ドキュメントレビュー（画像Provider統合・案A / spec02）— [`21-doc-image-provider.md`](21-doc-image-provider.md)（Blocker/Major 0、Minor 1。`HuggingFace__MediaRefetchAllowedHosts`（既定 `fal.media;replicate.delivery`・画像/動画共通・ラベル境界一致・空=全拒否）が BotOptions.cs と .env.example/README×2/CLAUDE.md/spec/テストAC#10 で完全一致。CLAUDE.md「HF Inference」本文の JSON(URL) 両対応＋SSRF ガード改訂が実装と整合。エンドポイント・`dotnet test`(36件)・秘密情報も整合。残 Minor＝内部仕様§3表の「サフィックス一致」表現は本レビューで「ラベル境界一致」へ統一済み。spec02の4ゲート全PASS）
- 2026-08-15 **PASS** セキュリティレビュー（画像Provider統合・案A / spec02）— [`20-security-image-provider.md`](20-security-image-provider.md)（Critical/High/Medium 0、Low 2・Info 2、脆弱パッケージ0。SSRF必須統制(scheme=https/allowlistラベル境界/空=全拒否フェイルクローズ/no-auth/timeout)を実装・テスト実証。実装ゲート引継ぎのMajor#1=リダイレクトによるallowlist迂回は Image/Video両typed clientの `AllowAutoRedirect=false` で正式クローズ確認。userinfo@トリック無効・秘密情報非露出・署名検証(記録17)回帰なし。委譲: dotnet-claude-kit:security-scan / security-auditor）
- 2026-08-15 **PASS** 実装レビュー（画像Provider統合・案A / spec02）— [`19-impl-image-provider.md`](19-impl-image-provider.md)（Blocker 0、Major 1・Minor 3。ビルド0警告/0エラー・Roslyn診断0・テスト36緑。SSRF必須統制(scheme/allowlistラベル境界/フェイルクローズ/no-auth/timeout)は実装・テスト済み。唯一のMajorはHttpClientのAllowAutoRedirect未無効化=allowlistリダイレクトバイパスの上乗せ強化で、攻撃者誘発経路なし・資格情報漏えいなしのためブロックせず、securityゲートへクローズ引継ぎ。委譲: dotnet-claude-kit:code-review / Roslyn MCP接続あり）
- 2026-08-15 **PASS** ドキュメントレビュー（モードコンテキスト+リッチメニュー+i18n / spec03 3a）— [`18-doc-mode-richmenu.md`](18-doc-mode-richmenu.md)（Blocker/Major 0、Minor 3。App__Locale/App__RichMenuEnabled が実コードと全ドキュメントで一致。前回(13)Major の QuickReply 齟齬は 🔄/💬 実装＋添付で解消。✏️編集=3b は未実装として正しく表記）
- 2026-08-15 **PASS** セキュリティレビュー（モードコンテキスト+リッチメニュー+i18n / spec03 3a）— [`17-security-mode-richmenu.md`](17-security-mode-richmenu.md)（Critical/High/Medium 0、Low 1・Info 1、脆弱パッケージ0。署名検証は回帰なし・トークン非露出・postback入力は安全に無視・locale由来のパストラバーサルなし。SSRFは3a新経路なし）
- 2026-08-15 **PASS** 実装レビュー（モードコンテキスト+リッチメニュー+i18n / spec03 3a）— [`16-impl-mode-richmenu.md`](16-impl-mode-richmenu.md)（Blocker/Major 0、Minor 4。ビルド0警告/0エラー。UserStateStore並行性・冪等provisioning・Kiota判別子・i18nを確認。✏️編集/AC#5は仕様どおり3bへ後回し。Roslyn MCP未接続で手動フォールバック）
- 2026-08-15 **PASS** 仕様レビュー（モードコンテキスト+リッチメニュー+画像セッション+i18n）— [`15-spec-mode-richmenu.md`](15-spec-mode-richmenu.md)（初回 FAIL→修正反映で再レビュー PASS。Blocker 0、前回 Major 3・Minor 3＋残 Minor 1 を全反映。3a は spec02 非依存で着手可）
- 2026-08-15 **PASS** 仕様レビュー（画像Provider統合・案A）— [`14-spec-image-provider.md`](14-spec-image-provider.md)（Blocker 0、Major 2・Minor 1 は仕様へ反映済み。allowlist のラベル境界明文化と動画 JSON-URL 回帰防止を対応）
- 2026-08-15 **PASS** ドキュメントレビュー（全体統合点検）— [`13-doc-full-audit.md`](13-doc-full-audit.md)（Blocker 0、Major 1・Minor 2。CLAUDE.md の QuickReply 記述が未実装＝要修正だが再現性・設定整合は全一致で PASS）
- 2026-08-15 **PASS** セキュリティレビュー（全体統合点検）— [`12-security-full-audit.md`](12-security-full-audit.md)（Critical/High/Medium 0、Low 2・Info 3、脆弱パッケージ0。SSRFはトークン非同送・video既定オフでLow止まり）
- 2026-08-15 **PASS** 実装レビュー（全体統合点検）— [`11-impl-full-audit.md`](11-impl-full-audit.md)（Blocker/Major 0、Minor 5。ビルド警告0/エラー0。Roslyn MCP 未接続のため手動フォールバック）
- 2026-08-14 **PASS** ドキュメントレビュー（image/video/docker）— [`10-doc-image-video-docker.md`](10-doc-image-video-docker.md)（Minor 2件を修正）
- 2026-08-14 **PASS** セキュリティレビュー（image/video/docker）— [`09-security-image-video-docker.md`](09-security-image-video-docker.md)（Low 1・Info 2、脆弱パッケージ0）
- 2026-08-14 **PASS** 実装レビュー（image/video/docker）— [`08-impl-image-video-docker.md`](08-impl-image-video-docker.md)（Minor 4、OCE 除外を修正）
- 2026-08-14 **PASS** ドキュメントレビュー（chat増分）— [`07-doc-chat.md`](07-doc-chat.md)（初回 FAIL→`ChatEndpoint` 記載を追記して PASS）
- 2026-08-14 **PASS** セキュリティレビュー（chat増分）— [`06-security-chat.md`](06-security-chat.md)（Critical/High/Medium なし。worker タイムアウトを追加対応）
- 2026-08-14 **PASS** 実装レビュー（chat増分）— [`05-impl-chat.md`](05-impl-chat.md)（Blocker/Major なし、Minor 3。worker タイムアウトを追加対応）
- 2026-08-14 **PASS** ドキュメントレビュー（phase2-scaffold）— [`04-doc-phase2-scaffold.md`](04-doc-phase2-scaffold.md)（初回 FAIL→設定キー整合を修正して PASS）
- 2026-08-14 **PASS** セキュリティレビュー（phase2-scaffold）— [`03-security-phase2-scaffold.md`](03-security-phase2-scaffold.md)（Critical/High/Medium なし、脆弱パッケージ0）
- 2026-08-14 **PASS** 実装レビュー（phase2-scaffold）— [`02-impl-phase2-scaffold.md`](02-impl-phase2-scaffold.md)（Blocker/Major なし、Minor 4）
- 2026-08-14 **PASS** 仕様レビュー — [`01-spec-line-hf-bot.md`](01-spec-line-hf-bot.md)（Major #1 タイムアウトを仕様反映）
